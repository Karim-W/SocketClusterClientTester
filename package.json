{
  "name": "socketclusterclienttester",
  "version": "1.0.0",
  "description": "Socket Cluster client which will be used to test connecting to a socket cluster client",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc -w | nodemon dist/index.js",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -p . && node dist/index.js"
  },
  "author": "karim-w",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.14",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "dotenv": "^16.0.0",
    "joi": "^17.6.0",
    "socketcluster-client": "^16.0.4"
  }
}